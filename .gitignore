# Claude Code local settings
.claude/

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code settings
.vscode/

# Firebase / Google services secrets
google-services.json
GoogleService-Info.plist

# Generated by `flutterfire configure`. Ignored because this repository is
# public: the file carries the Firebase API key and project identifiers, and
# Flutter's own docs say not to commit it when the app calls Gemini from the
# client, since anyone could reuse the config against the AI endpoint and burn
# the quota. App Check makes that much harder, but harder is not impossible and
# ignoring the file costs nothing. Same treatment as key.properties: absent on
# a fresh clone, regenerated by `flutterfire configure`.
lib/firebase_options.dart

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Scratch working copies — not part of the build
files/
files2/
graphify-out/
.p10/
.p12/
.p13/
.p14/
.p15/
.p16/
.p17/
.p18/
.p19/

# Release signing — NEVER commit these.
# key.properties holds the keystore password in plain text, and the .jks is the
# signing identity itself. Anyone with both can publish an update that Play and
# every installed device will accept as genuine, and the key cannot be rotated
# for an existing app listing.
android/key.properties
key.properties
*.jks
*.keystore
