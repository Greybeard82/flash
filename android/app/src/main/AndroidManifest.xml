<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- TV / Google TV support -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <!-- Touch is not available on TV; mark as not required so the app passes TV review -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>

    <!-- ML Kit GenAI requires API 26+ but we handle unavailability gracefully at runtime -->
    <uses-sdk tools:overrideLibrary="com.google.mlkit.genai.prompt,com.google.mlkit.genai.common"/>

<application
        android:label="Flash"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!--
              Android 16 (API 36) ignores an app's orientation lock on large
              screens and lets the system decide. This opts back out, which is
              what keeps the landscape lock in MainActivity working on a
              tablet running 16.

              It is a deliberate stay of execution, not a fix: the opt-out is
              ignored once the app targets API 37. Portrait tablet layouts
              have to exist before that move: see the PRD, "Decided, not
              built".
            -->
            <property
                android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
                android:value="true" />
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- TV launcher entry -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name="dev.fluttercommunity.workmanager.BackgroundWorker"
            android:exported="false"
            tools:ignore="Instantiatable"/>

        <!-- 1x1 unread-count widget. Not exported: the only thing that sends
             it an update is HomeWidget.updateWidget from our own process, plus
             the OS's own APPWIDGET_UPDATE. updatePeriodMillis is 0 in
             widget_unread_info.xml on purpose — every refresh is pushed on a
             real change, so a periodic wake would only re-render a number that
             had not moved. -->
        <receiver
            android:name=".UnreadWidgetProvider"
            android:exported="false"
            android:label="@string/widget_unread_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_unread_info" />
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <!-- Settings > About > Email us. Without this, package visibility on
             Android 11+ hides every mail app from the launch, and the row
             silently does nothing. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
